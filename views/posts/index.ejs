<!DOCTYPE html>
<html>
  <head>
    <%- include('../partials/head') %>
  </head>
  <body style="background-color:unset;">
    <%- include('../partials/nav') %>

    <div class="searchbar container mb-3">
      <form action="/posts" method="get" class="post-index-tool">
        <div class="form-row">

          <div class="form-group col-3">
            <select name="limit" class="custom-select" style="color:#B97BE3;" onchange="this.parentElement.parentElement.parentElement.submit()">
              <option value="5" <%= limit==5?'selected':'' %>>5 posts</option>
              <option value="10" <%= limit==10?'selected':'' %>>10 posts</option>
              <option value="20" <%= limit==20?'selected':'' %>>20 posts</option>
            </select>
          </div>

          <div class="form-group col-9">
            <div class="input-group">
              <select name="searchType" class="custom-select" style="color:#B97BE3;">
                <option value="title,body" <%= searchType=='title,body'?'selected':'' %>>제목+내용</option>
                <option value="title" <%= searchType=='title'?'selected':'' %>>제목</option>
                <option value="body" <%= searchType=='body'?'selected':'' %>>내용</option>
                <option value="author" <%= searchType=='author'?'selected':'' %>>저자</option>
                <option value="category" <%= searchType=='category'?'selected':'' %>>카테고리</option>
              </select>
              <input minLength="3" type="text" name="searchText" value="<%= searchText %>">
              <div class="input-group-append">
                <button class="btn btn-info" type="submit">Search</button>
              </div>
            </div>
          </div>

        </div>
      </form>
      <table class="board-table table table-light table-bordered border-bottom" style="background-color: #FCE0E5;">

        <thead class="thead" style="background-color: #FFB7C5; color:white;">
          <tr>
            <th scope="col" class="category" id="mdCategory">Category</th>
            <th scope="col" class="title">Title</th>
            <th scope="col" class="date" id="mdDate">Date</th>
          </tr>
        </thead>

        <tbody>
          <% if(posts == null || posts.length == 0){ %>
            <tr>
              <td colspan=5>아직 작성된 게시글이 없습니다.</td>
            </tr>
          <% } %>
          <% posts.forEach(function(post) { %>
            <tr>
              <td class="category" id="mdCategory">
                <a href="/posts<%= getPostQueryString(false, { searchType:'category', searchText:post.category }) %>">
                  <div data-search-highlight="category" class="ellipsis" style="font-weight: bolder; font-family:'Malgun Gothic'; color:#B97BE3;"><%= post.category %></div>
                </a>
              </td>
              <td>
                <a href="/posts/<%= post._id %><%= getPostQueryString() %>" class="title-container">
                  <div data-search-highlight="title" class="title-ellipsis ellipsis float-left">
                    <span class="title-text" style="font-weight: bolder; font-family:'Malgun Gothic'; color:#B97BE3;"><%= post.title %></span>
                  </div>
                  <% if(post.commentCount){ %>
                    <small class="title-comments d-inline-block pl-2" style="color:#B97BE3;">(Reply : <%= post.commentCount %>)</small>
                  <% } %>
                </a>
              </td>
              <td class="date" id="mdDate">
                <span data-date="<%= post.createdAt %>" style="font-weight: bolder; font-family:'Malgun Gothic'; color:#B97BE3;"></span>
              </td>
            </tr>
          <% }) %>
        </tbody>

      </table>

      <div class="row mb-3">

        <div class="col-2">
          <% if(isAdminLoggedin){ %>
            <a class="btn btn-info" href="/posts/new<%= getPostQueryString() %>">New</a>
          <% } %>
        </div>

        <nav class="col-8">
          <%
            var offset = 2;
            var previousBtnEnabled = currentPage>1;
            var nextBtnEnabled = currentPage<maxPage;
          %>
          <ul class="pagination pagination-sm justify-content-center align-items-center h-100 mb-0">
            <li class="page-item <%= previousBtnEnabled?'':'disabled' %>">
              <a class="page-link" href="/posts<%= getPostQueryString(false, {page:currentPage-1}) %>" <%= previousBtnEnabled?'':'tabindex=-1' %>>&laquo;</a>
            </li>
            <% for(i=1;i<=maxPage;i++){ %>
              <% if(i==1 || i==maxPage || (i>=currentPage-offset && i<=currentPage+offset)){ %>
                <li class="page-item <%= currentPage==i?'active':'' %>"><a class="page-link" href="/posts<%= getPostQueryString(false, {page:i}) %>"> <%= i %> </a></li>
              <% } else if(i==2 || i==maxPage-1){ %>
                <li><a class="page-link">...</a></li>
              <% } %>
            <% } %>
            <li class="page-item <%= nextBtnEnabled?'':'disabled' %>">
              <a class="page-link" href="/posts<%= getPostQueryString(false, {page:currentPage+1}) %>" <%= nextBtnEnabled?'':'tabindex=-1' %>>&raquo;</a>
            </li>
          </ul>
        </nav>

      </div>

      

    </div>
  </body>
  <script>
    $(window).on('load', function () {
        $('body').sakura();
    });
  </script>
</html>
